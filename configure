#!/bin/bash

CONFIG_DEFAULT="config.default.xcconfig"     
LOCAL_CONFIG_DEFAULT="config.xcconfig"

if [ ! -f $LOCAL_CONFIG_DEFAULT ]; then
  echo '#include "config.default.xcconfig"' | cat - $CONFIG_DEFAULT > temp && mv temp $LOCAL_CONFIG_DEFAULT
fi